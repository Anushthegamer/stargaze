# Build-time download cache (~14 MB of upstream catalogues)
tools/.cache/

# Python
__pycache__/
*.py[cod]
.venv/

# Locally minted TLS certificates
server/.cert/

# Node
node_modules/
dist/
.vite/
*.log

# Capacitor / Android
# The android/ project is committed; its build output and the copy of the web
# bundle that `cap sync` drops into it are not.
android/app/src/main/assets/public/
android/app/build/
android/build/
android/.gradle/
android/local.properties
*.apk
*.keystore

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
